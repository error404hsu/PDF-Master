name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: "Python ${{ matrix.python-version }}"
    runs-on: ubuntu-latest
    # 提早強制 Node.js 24，避免 Actions 的過期警告
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      # === 新增：安裝 Ubuntu 系統底層缺乏的 Qt (PySide6) 依賴 ===
      - name: Install Qt system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libegl1 libxkbcommon-x11-0 libxcb-cursor0 libxcb-icccm4 libxcb-keysyms1 libxcb-shape0

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # 以 pyproject.toml 為單一真實來源，安裝全部依賴（含 dev 群組）
          pip install -e ".[dev]"

      - name: Run pytest
        env:
          # offscreen 平台避免無螢幕環境 Qt 崩潰
          QT_QPA_PLATFORM: offscreen
        run: |
          pytest --tb=short -q

      - name: Run ruff check
        # 設定來源：pyproject.toml [tool.ruff]，不重複配置
        run: ruff check .

      - name: Run mypy
        # strict mode 已在 pyproject.toml [tool.mypy] 定義
        run: mypy core/ adapters/
